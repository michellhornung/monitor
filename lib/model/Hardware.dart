import 'package:flutter/material.dart';

class Hardware {
  final Color colour;
  final IconData icon;
  final String title;

  const Hardware({
    @required this.colour,
    @required this.icon,
    @required this.title,
  });
}
